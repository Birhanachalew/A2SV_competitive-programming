class Solution:
    def targetIndices(self, nums: List[int], target: int) -> List[int]:
        a=[]
        num=sorted(nums)
        for i in range(len(num)):
            if num[i]==target:
                a.append(i)
        return a
