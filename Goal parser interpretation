class Solution:
    def interpret(self, command: str) -> str:
        goal = {"()":"o","(al)":"al"}

        for char in goal.keys():
            command = command.replace(char,goal[char])
        return command

        



        


