class Solution:
    def singleNumber(self, nums: List[int]) -> int:
        return (sum(list(set(nums))*3)-sum(nums))//2
        
        """
        xor = 1
        for num in nums:
            xor ^= num
        return  xor

        """
        
