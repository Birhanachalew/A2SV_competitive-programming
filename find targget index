class Solution:
    def targetIndices(self, nums: List[int], target: int) -> List[int]:
        new = []
        nums = sorted(nums)

        for i in range(len(nums)):
            if nums[i] == target:
                new.append(i)
        return new
