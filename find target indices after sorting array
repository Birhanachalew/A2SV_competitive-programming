class Solution:
    def targetIndices(self, nums: List[int], target: int) -> List[int]:
        nums.sort()
        new=[]
        for i,Y in enumerate(nums):
            if Y == target:
                new.append(i)
        return new
        
